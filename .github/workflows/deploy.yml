name: Linux Container Node Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: azure/docker-login@v1
      with:
        login-server: helloactix.azurecr.io
        username: ${{ secrets.REGISTRY_USERNAME }}
        password: ${{ secrets.REGISTRY_PASSWORD }}
    - run: |
        docker build . -t helloactix.azurecr.io/helloactix:${{ github.sha }} -f "docker/app/Dockerfile"
        docker push helloactix.azurecr.io/helloactix:${{ github.sha }}
    - uses: azure/webapps-deploy@v2
      with:
        app-name: 'helloactix'
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        images: 'helloactix.azurecr.io/helloactix:${{ github.sha }}'